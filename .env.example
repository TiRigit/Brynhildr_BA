# Projekt: Brynhildr_BA
# Kopiere zu .env: cp .env.example .env && chmod 600 .env

# Datenbank (Schema in bestehender PostgreSQL-Instanz)
DATABASE_URL=postgresql://postgres:CHANGE_ME@127.0.0.1:5432/postgres?schema=Brynhildr_BA
DB_SCHEMA=Brynhildr_BA

# App
PORT=3000
NODE_ENV=development
